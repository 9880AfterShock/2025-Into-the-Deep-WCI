package org.firstinspires.ftc.teamcode
//imports
import com.acmerobotics.roadrunner.Pose2d


// Poses
    // blue side
    var startPoseBlue = Pose2d(20.0, 61.0, 0.0)
    var clipPoseBlue = Pose2d(0.0, 34.0, Math.toRadians(45.0))
    var parkPoseBlue = Pose2d(-60.0, 60.0, Math.PI * 1.5)
    // red side
    var startPoseRed = Pose2d(15.0, -61.0, Math.toRadians(180.0))
    var clipPoseRed = Pose2d(0.0, -34.0, Math.toRadians(180.0))
    var parkPoseRed = Pose2d(60.0, -60.0, Math.toRadians(90.0))
// Paths